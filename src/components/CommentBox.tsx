import React from 'react';
import styled from 'styled-components';

const Wrapper = styled.div`
	background-color: blue;
	height: 600px;
`;
const CommentBox = () => {
	return <Wrapper>CommentBox</Wrapper>;
};

export default CommentBox;
